def RetValue(char1,sum,char2):
	sum=sum - (int(char1)-64)*(10**2)
	sum=(sum*10)+(int(char2)-64)
	return sum
def binary_Search():
	b=input ("Enter a string through which you want to check for repeated activities")
def rolling_hash( a,index,size):
	sum=0
	c=size-1
	if index ==len (a)-1:
		return c
	else :
		if index==0:
			while (index!=size):